<UserControl
    x:Class="Usefull_Tools_UI.Console.ConsoleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Usefull_Tools_UI.Console"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ConsoleUItextBox" Foreground="White" Grid.Row="0"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="ConsoleUIReadBox" Grid.Column="0" />
            <Button x:Name="ConsoleUIbtReturn" Content="return" Click="ConsoleUIbtReturn_Click" Foreground="White" HorizontalAlignment="Center" Grid.Column="1"/>
        </Grid>

    </Grid>
</UserControl>
